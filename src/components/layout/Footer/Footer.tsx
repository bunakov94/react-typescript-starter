import React from 'react';

import style from './Footer.module.scss';

const Footer: React.FC = () => (
  <footer>
    <h2 className={style.header}>Hello from Footer</h2>
  </footer>
);

export default Footer;
